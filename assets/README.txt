Coloca aquí los archivos de logo que subiste como adjuntos en la conversación.

Nombres esperados (se aceptan SVG ahora):
- `logo-white.png` o `logo-white.svg` -> versión blanca/monocromo para mostrar en la cabecera (transparente preferible)
- `logo-color.png` or `logo-color.svg` -> versión a color para favicon y usos secundarios

Sugerencias de tamaño:
- `logo-white.png`: 600x600 (o similar), fondo transparente
- `logo-color.png`: 256x256 (favicon), fondo transparente o color sólido

Pasos:
1. Guarda las imágenes adjuntas desde la conversación con los nombres anteriores, o usa los SVG ya creados en esta carpeta.
2. Colócalas en la carpeta `assets/`.
3. Refresca el navegador para ver los cambios.

Si quieres, puedo incrustar las imágenes directamente en `index.html` como data-URI si me confirmas que quieres que lo haga ahora.